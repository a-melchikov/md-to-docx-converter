# Docker assets

Dockerfiles for production-like and dev container targets live in this directory:

- `Dockerfile.api`
- `Dockerfile.web`

Use root `docker-compose.yml` for production-like startup and `docker-compose.dev.yml` for dev startup.
